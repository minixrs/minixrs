/review xhigh 54

---

<task-notification>
<task-id>af2ce762eab9361e8</task-id>
<output-file>/private/tmp/claude-501/-Users-kevinbarnard-src-minixrs/62b598fb-4bac-42eb-bc0e-bd2735836059/tasks/af2ce762eab9361e8.output</output-file>
<status>completed</status>
<summary>Agent "/code-review xhigh 54" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have the full picture. Here are the findings.

```json
[
  {
    "file": "fs/mfs/src/main.rs",
    "line": 865,
    "summary": "create() reintroduces the exact allocate-then-fail leak the slice's headline fix removes from do_write: alloc_inode + write_inode run before insert_entry, whose place_zone / dir_append_offset can still answer ENOSPC, orphaning one inode per attempt with no way to reclaim it.",
    "failure_scenario": "A client fills the image with legitimate writes until zones are exhausted, then loops open(path, O_CREAT) on a directory that needs a new block (or one at MAX_DIR_BYTES). Each call reaches insert_entry -&gt; place_zone -&gt; alloc_zone -&gt; ENOSPC after alloc_inode has already set the imap bit, so one of the 128 inodes is permanently burned per attempt. After ~128 iterations no file can be created for the rest of the boot, even after truncating files to free every zone — a state the zone-side fix explicitly made unreachable. The staging discipline is available here too (find_free_slot already runs first; the directory's growth zone could be placed before alloc_inode), so this is the same defect one step later, not a different one."
  },
  {
    "file": "fs/mfs/src/main.rs",
    "line": 172,
    "summary": "`Stage` is a freely-constructible unit struct, so its documented \"created exactly once in main, and the borrow checker is what keeps it the only one\" aliasing guarantee is convention rather than type-enforced — unlike `Blocks`, whose private fields force construction through `device()`.",
    "failure_scenario": "Any future edit writing `let mut s2 = Stage;` (a one-word mistake, since the type needs no arguments) obtains a second independent `&amp;mut` path to `static STAGE`. `let bytes = s1.fill(..)?;` followed by `s2.fill(..)` has the kernel's safecopy write the buffer while `bytes: &amp;[u8]` is still live — undefined behaviour in the one module whose whole design premise is that `&amp;mut self` serializes access to the static. Give `Stage` a private field (`struct Stage(());`) plus a `fn stage() -&gt; Stage` constructor, mirroring `Blocks`/`device()`."
  },
  {
    "file": "servers/vfs/src/main.rs",
    "line": 1262,
    "summary": "The new `trunc-dir` denial probe aims a real `FS_TRUNC` at `/etc`, violating CLAUDE.md's rule to \"never aim a 'must be refused' probe at a file or block whose accidental write would destroy something\" — the slice's own `create-exists` probe demonstrates the safe pattern.",
    "failure_scenario": "If MFS's `node.is_dir()` guard in do_trunc is ever weakened or reordered, this probe frees /etc's directory zones at boot, destroying /etc/motd, /etc/pattern, /etc/scratch, /etc/holey and /etc/deny in one call — taking fs.selfcheck, fs.read, fs.write, fs.trunc, fs.create, fs.hole and fs.leak down together, so the log says nothing about which check regressed. The code comment concedes this (\"destructive, but *loud*\"), yet `ROOTFS_DENY_PATH` was created two probes earlier precisely because \"a probe that accidentally succeeded would corrupt no proof but its own\". A purpose-built empty directory carries the identical EISDIR proof with zero blast radius."
  },
  {
    "file": "fs/mfs/src/main.rs",
    "line": 1264,
    "summary": "write_inode's stated safety justification — \"every caller has already read this same inode through [read_inode], so the block it names was checked there\" — is falsified by this branch's own new caller, create(), which writes a freshly allocated inode that was never read.",
    "failure_scenario": "`create()` calls `write_inode(blocks, mount, ino, &amp;node)` for an `ino` that came straight out of `alloc_inode`; no `read_inode` (and therefore no `walk::zone_ok`) ever ran on it. The write happens to stay inside the inode table because `alloc_inode` bounds `ino` by `mount.ninodes` and `inode_location` bounds the block by `inode_blocks` — but that is a different, unstated chain, and the comment now reads as coverage for a check that no longer holds. This is precisely the \"doc comment true when written, falsified by a later task in the same branch\" class CLAUDE.md's 5.10b lesson names; a future caller trusting the sentence literally would reintroduce an unchecked device write."
  },
  {
    "file": "fs/mfs/src/main.rs",
    "line": 1065,
    "summary": "alloc_zone's doc still directs the reader to do_write \"for why that leak is a *reachable* denial of service rather than a benign one\" — a claim this very branch deleted from do_write, which now documents the leak as closed.",
    "failure_scenario": "A reader following the cross-reference lands on do_write's rewritten comment, which states the opposite (\"no client-controlled failure occurs after an allocation\"). The two comments now contradict each other in the same file, and the surviving one asserts a live security property that no longer exists — the same falsified-in-branch defect class as the `vfs_open_payload_offsets_are_ordered_and_disjoint` tripwire the PR description calls out."
  },
  {
    "file": "fs/mfs/src/main.rs",
    "line": 1232,
    "summary": "do_trunc zeroes the whole `zone: [u32; NR_TZONES]` array (10 slots) but free_zones_of only reclaims slots 0..7 plus the single-indirect chain, so a nonzero DOUBLE_INDIRECT_SLOT or slot 9 is silently discarded rather than freed or reported.",
    "failure_scenario": "An inode with `zone[8] != 0` (a double-indirect pointer — nothing in this tree writes one today, but the field exists and is decoded by `Inode::from_le_bytes` off the device) is truncated: `node.zone = [0; NR_TZONES]` drops the pointer, `free_zones_of` never looks at index 8, and the zone plus everything it names becomes unreclaimable with the bitmap still marking it in use, with no errno. Every other 'unreachable' state in this module answers EIO on purpose (alloc_inode's `ino == 0 || ino &gt; ninodes`, Stage::fill's `len &gt; MFS_BLOCK_SIZE`); this one answers success."
  },
  {
    "file": "fs/mfs/src/main.rs",
    "line": 1189,
    "summary": "free_zone's range check (`write::write_zone_ok(zone, first_data_zone, mount.blocks)`) permits ROOTFS_TAIL_BLOCK — the reserved last zone whose bitmap bit mkfs deliberately marks in use to protect the image's tail label — so the new free path can hand that block back to the allocator.",
    "failure_scenario": "An inode naming zone 255 (mount.blocks - 1) reaches do_trunc; write_zone_ok(255, 6, 256) is true, so free_zone clears bit 250 and alloc_zone (whose limit is also mount.blocks) can then hand zone 255 to a file, whose first write overwrites the 32-byte tail label that MEM's init check and MFS's `tail_probe` exist to verify. verify.rs already counts free zones over `[first_data_zone, ROOTFS_TAIL_BLOCK)` — the runtime side has no equivalent upper bound, and the free path is what makes the gap newly exploitable."
  },
  {
    "file": "servers/vfs/src/main.rs",
    "line": 626,
    "summary": "do_open issues FS_TRUNC before fd::alloc, so a descriptor-table-full open reports EMFILE with the file already truncated.",
    "failure_scenario": "A process with all 8 rows of its fd table in use calls open(path, O_RDWR | O_TRUNC). classify succeeds, fs_trunc frees every zone of the file, then fd::alloc returns EMFILE and do_open returns it. The caller sees a failed open and has no reason to believe anything changed, but the file's contents are gone. Linux allocates the descriptor first for exactly this reason; the doc comment above the block reasons only about the converse case (a truncate failure leaving no descriptor)."
  },
  {
    "file": "kernel-shared/src/rootfs.rs",
    "line": 311,
    "summary": "`const _: () = assert!(ROOTFS_LEAK_PROBES &gt;= ROOTFS_IMAGE_BLOCKS as usize)` restates the definition written two lines above it (`ROOTFS_LEAK_PROBES = ROOTFS_IMAGE_BLOCKS as usize`), so it cannot enforce the property its comment claims (\"must out-number any free-zone count the image can have\").",
    "failure_scenario": "The comment reads as a guard on the probe's config-independence, but the only edit it catches is replacing the constant's *value*; it says nothing about the free-zone count it is supposed to dominate, and it holds identically whether the image leaves 1 free zone or 250. This is the tautological-cap-assert class CLAUDE.md's 5.10a lesson names. The real invariant is checkable where the free-zone count is already known: kernel/build.rs computes `free = verify::free_zones(&amp;img)` and could assert `free &lt; ROOTFS_LEAK_PROBES` beside its existing headroom check."
  },
  {
    "file": "kernel/Cargo.toml",
    "line": 63,
    "summary": "A whole new crate dependency (`minixrs-mfs`) is added to the kernel purely to name `MFS_BLOCK_SIZE` in build_rootfs, when build_rootfs already imports from `minixrs_kernel_shared::rootfs`, which defines ROOTFS_HOLEY_LEN as `2 * BDEV_BLOCK_SIZE`.",
    "failure_scenario": "Every kernel build now host-compiles the entire minixrs-mfs library (proto/walk/write/read/dirent/inode/layout) as a build-dependency, and Cargo.lock gains an edge kernel -&gt; fs/mfs, to obtain one integer that is const-asserted equal to a constant already in scope. A one-line `pub const ROOTFS_HOLEY_HOLE: usize = BDEV_BLOCK_SIZE;` beside ROOTFS_HOLEY_LEN in kernel-shared/src/rootfs.rs gives the same value with no new dependency, and keeps the hole size and the file length defined together where the `const _` that relates them already lives."
  },
  {
    "file": "fs/mfs/src/walk.rs",
    "line": 417,
    "summary": "The `driving_next_dir_chunk_over_a_three_block_directory_converges_on_the_size` test comment still claims \"The arm no image reaches — mkfs-mfs never builds a directory past one block — so this is the only thing that will ever run it\", which this branch falsified.",
    "failure_scenario": "The sibling doc comment on next_dir_chunk itself (walk.rs:138-143) was updated by this branch to say the second iteration is now reached at boot via /full's runtime growth and the fs.dirgrow probe; this test comment, five lines of prose asserting the opposite, was not. A reader deciding whether the multi-block scan has boot coverage gets contradictory answers from the same file — the same defect class as the four other in-branch falsifications the PR description enumerates."
  },
  {
    "file": "fs/mfs/src/layout.rs",
    "line": 121,
    "summary": "zmap_bit keeps a bare `first_data_zone - 1` and the diff adds a comment arguing why it cannot underflow, instead of using checked_sub — against CLAUDE.md's rule for fs crates and against alloc_zone's own precedent for the identical expression.",
    "failure_scenario": "CLAUDE.md: \"Offset/length arithmetic in server-rt and in any server/driver/fs crate (servers/, drivers/, fs/, userland/) must use checked_add, not +: [profile.release] sets overflow-checks = false, so `off + 4` on a large offset wraps in the shipped binary ... while panicking under cargo test.\" fs/mfs/src/main.rs:1078 spells the same `first_data_zone - 1` as `checked_sub(1).ok_or(EIO)?` with a comment saying exactly why the bare form is unsafe here. With first_data_zone = 0 this function wraps silently in the shipped server and panics in mkfs's host tests — the two-profile divergence the rule exists to eliminate. free_zone (new this slice) is now a caller."
  },
  {
    "file": "userland/init/src/main.rs",
    "line": 1115,
    "summary": "trunc_demo's failure report flattens every negative errno to `off=0` via `n.max(0) as usize`, discarding the one piece of information the FAIL line exists to carry.",
    "failure_scenario": "If the post-truncate read answers EIO, EBADF or EINVAL, init prints `minix.rs init: fs.trunc FAIL read off=0` — identical for all three, and identical in shape to a report of a real byte offset. The forbidden marker still fires, so CI goes red, but the log names neither the errno nor a real offset. report_at's whole rationale (\"The offset is what makes the line worth more than its absence\") does not apply to a value that is only ever 0 or an errno; report_step with a distinct name, or appending the errno, would keep the diagnostic."
  }
]
```</result>
<usage><subagent_tokens>280486</subagent_tokens><tool_uses>47</tool_uses><duration_ms>947677</duration_ms></usage>
</task-notification>

---

Go ahead and fix all of them

---

commit and psuh

---

apply all